def isPalindrome(self,s: str) ->bool:
srts = []
for i in word:
    if i.isalnum():
        srts.append(i.lower())
        
while len(srts)>1:
    if srts.pop(0) != srts.pop(): 
        return False
   
return True   
